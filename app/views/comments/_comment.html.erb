<div>
	<%= comment.position.to_s %>. <%= link_to h(comment.user.username), user_path(comment.user) %>
	<%= comment.created_at.strftime("%a, %b %e %I:%M %p") %><br />
	<%=h comment.content %>
</div>
<hr />
